import type { ReportRow } from "@relead/domain/import-export/export/types";

export function exportRowsToPdf(rows: ReportRow[]) {
  void rows;
  throw new Error("PDF exporter pendiente: requiere librería de render estable.");
}
