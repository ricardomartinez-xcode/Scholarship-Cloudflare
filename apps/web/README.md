# @relead/web

Este workspace define la estructura objetivo para migrar progresivamente la app actual (`/src`) hacia `apps/web/src` sin romper rutas ni backend.

## Estado actual
- La app productiva sigue en `src/` para preservar estabilidad.
- Esta carpeta contiene la estructura base de destino y documentación de transición.
