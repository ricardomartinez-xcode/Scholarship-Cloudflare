import {describe,expect,it} from "vitest";import {buildStudentSummary,mexicoLocalToUtc,parseFlowResponse} from "./contract";const base={event_id:"e1",submission_action:"open_chat_now",student_name:"Ana"};
describe("WhatsApp Flow contract",()=>{it("parses nfm_reply",()=>expect(parseFlowResponse(JSON.stringify(base)).event_id).toBe("e1"));it("builds chat-now summary",()=>expect(buildStudentSummary(base as never)).toContain("Ana"));it("rejects past date",()=>expect(()=>mexicoLocalToUtc("2020-01-01","10:00",new Date("2026-01-01"))).toThrow("scheduled_time_in_past"));it("requires call date",()=>expect(()=>parseFlowResponse(JSON.stringify({...base,submission_action:"schedule_call"}))).toThrow("scheduled_date_time_required"));it("requires chat date",()=>expect(()=>parseFlowResponse(JSON.stringify({...base,submission_action:"schedule_chat_followup"}))).toThrow("scheduled_date_time_required"));it("requires payment description",()=>expect(()=>parseFlowResponse(JSON.stringify({...base,submission_action:"schedule_payment_help",scheduled_date:"2027-01-01",scheduled_time:"10:00"}))).toThrow("problem_description_required"));it("rejects CVV",()=>expect(()=>parseFlowResponse(JSON.stringify({...base,cvv:"123"}))).toThrow("sensitive_payment_data_rejected"));it("accepts unsubscribe",()=>expect(parseFlowResponse(JSON.stringify({...base,submission_action:"unsubscribe"})).submission_action).toBe("unsubscribe"));it("rejects incomplete new student",()=>expect(()=>parseFlowResponse(JSON.stringify({event_id:"e",submission_action:"open_chat_now",student_type:"new"}))).toThrow("new_student_incomplete"));it("rejects incomplete returning student",()=>expect(()=>parseFlowResponse(JSON.stringify({event_id:"e",submission_action:"open_chat_now",student_type:"returning"}))).toThrow("returning_student_incomplete"))});
