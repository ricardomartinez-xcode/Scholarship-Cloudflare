import {NextResponse} from "next/server";import {assertMetaWebhookSignature,handleWhatsappWebhook,verifyMetaWebhookHandshake} from "@/lib/whatsapp-flow/webhook";export const runtime="nodejs";export const dynamic="force-dynamic";
export async function GET(r:Request){const q=new URL(r.url).searchParams;try{return new NextResponse(verifyMetaWebhookHandshake({mode:q.get("hub.mode"),verifyToken:q.get("hub.verify_token"),challenge:q.get("hub.challenge")}))}catch{return NextResponse.json({ok:false},{status:403})}}
export async function POST(r:Request){const raw=await r.text();try{assertMetaWebhookSignature(raw,r.headers.get("x-hub-signature-256"));return NextResponse.json({ok:true,...await handleWhatsappWebhook(raw)})}catch(e){return NextResponse.json({ok:false,error:e instanceof Error?e.message:"webhook_failed"},{status:401})}}
