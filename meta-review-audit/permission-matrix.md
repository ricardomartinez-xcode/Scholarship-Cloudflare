# Permission Matrix

| permission_or_feature | official_allowed_use | exact_Recalc_behavior_that_would_justify_it | WABA_tab_surface_area | backend_routes_or_services_required | reviewer_visible_flow_required | current_status_in_repo | needed_or_not_needed | blockers |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `whatsapp_business_messaging` | Real WhatsApp Business send/receive operations, message status handling, and media usage on the WhatsApp Business Platform. https://developers.facebook.com/documentation/business-messaging/whatsapp/permissions/ https://developers.facebook.com/documentation/business-messaging/whatsapp/business-phone-numbers/media/ | Send a text/template/media message from Recalc to a real contact and show persisted outbound history plus webhook-driven delivery evidence. | `Send action`, `Recent messages`, `Webhook and delivery evidence`, reviewer contact picker. | `src/lib/meta-whatsapp.ts`, `src/lib/user-contacts.ts`, `src/app/api/integrations/meta/whatsapp/send/route.ts`, `src/app/api/integrations/meta/whatsapp/media/route.ts`, `src/app/api/integrations/meta/webhook/route.ts`, `src/app/api/integrations/meta/whatsapp/overview/route.ts` | Open WABA tab, pick contact, send message/media, refresh/sync, show persisted message row and status/event evidence. | Implemented in code in this patch; not demo-ready because Meta env, reviewer account, target contact, and webhook credentials are missing. | Needed | Missing local and Vercel Meta env, no reviewer-safe WABA connection, no reviewer-safe contact/media fixture, no webhook verify token, no E2E auth credentials. |
| `whatsapp_business_management` | Real WABA setup and asset management behavior such as embedded signup, phone number context, WABA/account reads, official template listing, and business profile/asset reads. https://developers.facebook.com/documentation/business-messaging/whatsapp/permissions/ https://developers.facebook.com/documentation/business-messaging/whatsapp/templates/overview | Launch embedded signup, sync WABA assets, show business/WABA/phone metadata, list official Meta templates, and display business profile context. | `Connection status`, `Business assets`, `Official Meta templates`, `Launch Embedded Signup`, `Sync from Meta`. | `src/lib/meta-whatsapp.ts`, `src/app/api/integrations/meta/exchange-code/route.ts`, `src/app/api/integrations/meta/connection/route.ts`, `src/app/api/integrations/meta/whatsapp/overview/route.ts` | Open WABA tab, launch embedded signup or use existing connection, sync assets, show WABA metadata and official templates. | Implemented in code in this patch; reviewer flow blocked by missing public/server Meta env and missing live connected WABA. | Needed | Missing `META_APP_ID`, `META_APP_SECRET`, `META_INTEGRATION_SECRET`, `NEXT_PUBLIC_META_APP_ID`, `NEXT_PUBLIC_WHATSAPP_EMBEDDED_CONFIG_ID`; no live reviewer account connection. |
| `business_management` | Business Manager asset access outside the WhatsApp-specific permissions. The docs do not support claiming it unless the app truly needs Business Manager assets. https://developers.facebook.com/docs/permissions/ https://developers.facebook.com/documentation/facebook-login/facebook-login-for-business | No real Recalc behavior currently justifies it. The WABA work is intentionally scoped to WhatsApp-specific assets only. | None intentionally exposed. | None intentionally implemented. | No valid reviewer flow exists today. | Not implemented on purpose. | Not needed currently | No in-product Business Manager action exists; requesting it would be over-scoped. |
| `Business Asset User Profile Access` | Limited access to business-user profile fields such as `id`, `ids_for_business`, `name`, and `picture`. https://developers.facebook.com/docs/features-reference/business-asset-user-profile-access/ | Store and display only the business user identity fields that Meta actually returns and Recalc actually uses for contact linkage or reviewer evidence. | `Identity and BSUID` panel. | `src/lib/meta-whatsapp.ts`, `src/lib/user-contacts.ts`, webhook processing, overview serializer. | Open WABA tab, show linked contact plus real business-scoped identity/profile fields and last sync time. | Minimal code support exists now, but no live data has been captured to prove a reviewer-visible end-to-end flow. | Conditional only if live data is truly used | No live BAUPA data in environment; no reviewer-safe proof that `id`, `ids_for_business`, `name`, or `picture` are actually returned. |
| `BSUID support` | Stable business-scoped identity handling for WhatsApp users where business-scoped IDs matter for durable mapping. https://developers.facebook.com/documentation/business-messaging/whatsapp/business-scoped-user-ids | Deduplicate and link contacts/conversations by BSUID or `wa_id` before falling back to phone, and expose the resulting linkage in the UI. | `Identity and BSUID` panel plus message/event association. | `prisma/schema.prisma`, migration `20260404_waba_app_review_foundation`, `src/lib/user-contacts.ts`, `src/lib/meta-whatsapp.ts` | Open WABA tab, select a contact, show BSUID/username/profile source, then correlate sends or webhook events to the same contact. | Implemented in code for storage, dedupe, and display; not demonstrable yet because no live BSUID payloads are available. | Needed for correctness when Meta sends BSUID data; not a standalone permission | No live webhook or business profile data has been captured; final BSUID video remains blocked. |
